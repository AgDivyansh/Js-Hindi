const user = {
    username: "Divyansh",
    loginCount: 8,
    signedIn: true,
    getUserDetails: function() {
        console.log(`fetching user details from the data base`);
        
    }
};
// console.log(user.getUserDetails());
user.getUserDetails() ;
